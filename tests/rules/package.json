{
  "name": "@studiqa/rules-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-studiqa \"vitest run\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "vitest": "^2.0.0",
    "firebase-tools": "^13.0.0"
  }
}
